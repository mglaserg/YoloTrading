[project]
name = "crypto-yolo-trading"
version = "0.5.0"
description = "Crypto YOLO pre-live trade planning, audit, and Hyperliquid execution staging"
requires-python = ">=3.11"

[project.scripts]
yolo-preview = "crypto_yolo.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
