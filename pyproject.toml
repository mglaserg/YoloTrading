[project]
name = "crypto-yolo-trading"
version = "0.6.1"
description = "Crypto YOLO live Hyperliquid execution, audit, and restart-safe reconciliation"
requires-python = ">=3.11"

[project.scripts]
yolo-preview = "crypto_yolo.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
