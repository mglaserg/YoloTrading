#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHON_BIN="${YOLO_PYTHON:-python3}"

export PYTHONPATH="$ROOT_DIR/src${PYTHONPATH:+:$PYTHONPATH}"
cd "$ROOT_DIR"
exec "$PYTHON_BIN" -m crypto_yolo.cli "$@"
